Estafeta Wrapper
=======================

This project serves as a wraper to Estafeta's API, it is implemented using
zeep for the interaction with the SOAP API, so the wrapper is compatible
with python 3.
